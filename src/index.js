import React from 'react';
import ReactDOM from 'react-dom';

class TodoApp extends React.Component {
    render() {
        return {
            <h2>Simple Todo App with React</h2>
        }
    }
}

ReactDOM.render(<TodoApp />, document.getElementById('app'));